# Server
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/notification_dispatcher

# App
APP_VERSION=0.1.0
LOG_LEVEL=info

# Auth - replace with a strong secret in production
API_KEY=dev-internal-key

# WhatsApp - must match the token you configure in Meta Developer Portal
WHATSAPP_VERIFY_TOKEN=dev-whatsapp-verify-token
